// returns a shallow copy of a portion of the given array (will not modify the actual array)
// you can also start from the end just like the slice method in python
const numbers = [1, 2, 3, 4, 5];
const nums2 = numbers.slice(1, 4);
