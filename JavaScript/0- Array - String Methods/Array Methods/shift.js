// Removes the first element from an array and returns it
// Opposite of pop method

const numbers = [1, 2, 3, 4, 5];
// removes an element (you can also store it)
const first = numbers.shift();
console.log(numbers, first);